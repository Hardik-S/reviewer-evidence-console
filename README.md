# Reviewer Evidence Console

Reviewer Evidence Console turns scattered engineering evidence into a compact proof packet for portfolio review.

This base README exists so the repository has a stable `main` branch before feature work begins in dedicated git worktrees. The implementation branch documents product decisions, setup, verification, and deployment details.
